const campoNome = document.getElementById('nome');
const formulario = document.getElementsByTagName('form');
const botao = document.getElementById('btn-enviar')

botao?.addEventListener('click', () => console.log("Enviado"))

