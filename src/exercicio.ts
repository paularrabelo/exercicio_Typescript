function multiplica (num1: number, num2: number): number {
    return num1 * num2
}

function saudacao (nome: string): string {
    return `Olá ${nome}`
}

console.log(multiplica(10, 20));
console.log(saudacao("Paula"));